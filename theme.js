import { createTheme } from '@mui/material/styles';

export const ikrusherTheme = createTheme({
  palette: {
    primary: {
      light: '#9db3ba',
      main: '#85a1a9',
      dark: '#5d7076',
      contrastText: '#fff',
    }
  },
});